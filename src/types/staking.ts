export interface StakingInfo {
  stakedTokens: string;
  availableStake: string;
  lockedTokens: string;
  unlockBlock: number;
  minimumStake: string;
  lockPeriod: number;
  hmtBalance: string;
  chainId: number;
}
